// nextron.config.js

module.exports = {
  // O ponto de entrada do seu processo principal (o código fonte)
  main: {
    // Certifique-se que o caminho aponta para o seu arquivo principal do Electron
    entry: 'main/background.ts',
  },
};
